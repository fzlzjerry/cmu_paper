# Risk register

Last updated: 2026-07-25 after Phase 6 blocked admission.

| ID | Risk and evidence | Potential impact | Required mitigation / gate | Status |
|---|---|---|---|---|
| R-001 | Formal E00 run `e00-20260722T041628.190813Z-980eff7b6f59-0dd71f2d` materialized a toolchain gap when SASS dumping could not find `nvdisasm`. The failed evidence remains immutable. | Build/inspection failure, unavailable native/PTX proof, invalid counters, or wrong code path. | Exact package/tool locking at 6442ba1f7554ea0ebf0b3bb1a920c94567cab689 and native rerun `e00-20260722T050632.375718Z-6442ba1f7554-02d5bd32` passed. Phase 6A container run `e00-20260724T195014.679255Z-a6025ae023e1-23dbe853` independently passed SASS/PTX, native/forced-PTX, numerical/Graph/allocation, and all sanitizer lanes in the Decision 0016 image. | mitigated for native-host and exact authorized-container G0 |
| R-002 | CUDA Graph support differs by method; KIVI/KVQuant references are dynamic, while vLLM TurboQuant only declares uniform-batch support. The Phase 3 BF16 fixed-shape lane passed 8/8 capture/replay, output-agreement, pointer-stability, and replay-allocation controls. | Capture failure or unfair graph/eager comparison. | Preserve the BF16 evidence, and require E04 plus each G2 to repeat capture/replay, output, pointer, and allocation validation for its own implementation; unsupported methods remain eager-only. | mitigated for the BF16 fixed-shape lane; method-specific risk open |
| R-003 | KIVI pinned source contains repeat_kv/expand-reshape paths; KVQuant GQA support is unestablished. | Physical KV replication and inflated traffic/memory. | GQA execution-path audit plus synthetic MHA control; reject materialization in Measurement Lane. | open |
| R-004 | vLLM TurboQuant has a continuation-prefill fallback that fully dequantizes cached K/V; other references may reconstruct large tensors. | False compression/speed conclusions and OOM. | Fixed-L trace must prove the fallback absent; G3 rejects any full-prefix BF16 temporary. | open |
| R-005 | KIVI pins Torch 2.1.2, Transformers 4.36.2, Triton 2.1.0, FlashAttention 2.5.6, and CUDA 12.1-era packages; KVQuant vendors Transformers 4.38.0.dev0. | Blackwell build/runtime incompatibility or accidental semantic drift during upgrades. | Isolate reference containers; record exact patches; do not modernize semantics silently. | open |
| R-006 | Full B/L grids up to B=16 and L=131072 may exceed 96GB after weights, cache, workspace, and safety margin. | OOM, selective missingness, or capacity/same-work confusion. | Precompute feasibility with max_memory_fraction=0.88 and record every infeasible point before launch. | open |
| R-007 | TurboQuant paper provides no author-owned repository; Phase 5 therefore selects official vLLM v0.25.1 commit `752a3a504485790a2e8491cacbb35c137339ad34` as authority only for the TurboQuant/vLLM lane, whose source explicitly omits QJL. | Fixtures could be overgeneralized to a different paper variant. | Preserve the bounded authority statement, exact source hashes, and configuration names. Any substitution or broader equivalence claim requires a decision record and new fixtures. | mitigated for Phase 5 vLLM conformance; broader paper equivalence open |
| R-008 | KVQuant dataset, calibration seed, quantizers, sink policy, dtypes, and outlier cap are not frozen. | Data leakage, changing bytes, and non-reproducible method semantics. | E09 freezes all calibration inputs/artifacts before adapter or scan work. | open |
| R-009 | Archive acquisition URL and history before workspace delivery are unknown, although local bytes are fully hashed. | External reacquisition cannot reproduce the identical bundle. | Obtain operator provenance if available; retain archive hash and every extracted-file hash permanently. | open |
| R-010 | Git repository was initially unborn and had no SHA. | No run could satisfy code-provenance requirements before a commit existed. | Resolved by reviewed root commit 9569d938d9023a3e71d98f12234efa1897004533; every later run still requires a clean recorded SHA. | resolved 2026-07-22 |
| R-011 | The exact primary model/tokenizer identity was formerly unresolved. Decision 0007 freezes `meta-llama/Llama-3.1-8B-Instruct` revision `0e9e39f249a16976918f6564b8830bc894c89659`, all 11 artifact hashes, 32/8 GQA geometry, and 131072 positions; offline BF16 load and all 20 native-host G1 runs passed the identity join. Phase 6A eager/graph parity repeated the exact model/tokenizer identity inside the Decision 0016 image. | Unsupported head geometry, context limit, or altered attention path. | Preserve the exact model, tokenizer, hashes, and offline-loading policy for every later run and quality join; a changed image or model identity requires new parity and authority. | resolved for frozen identity in native-host G1 and exact container parity; method-specific gates remain |
| R-012 | Profiler instrumentation changes timing. | Invalid latency claims. | Separate timing, nsys, and ncu run kinds and validate analysis filters. | controlled by contract |
| R-013 | Phase 3 preserves GPU identity, power, clocks, thermals, and continuous process evidence per run. Both complete post-B-016 campaigns retained the exact independent eager/graph replicate sets, and the immutable PASS report derives both host-wall stability summaries at CV <= 3%. | High CV, biased replicates, or incomplete stability evidence. | Preserve every replicate. These native-host values are admission controls only and remain ineligible for a performance claim; G5 remains NOT EVALUATED. | mitigated for native-host G1; claim-bearing timing risk open |
| R-014 | PDFs are untrusted binaries; qpdf is unavailable. Static checks found no JavaScript or attachments. | Parser exposure or incomplete active-content assessment. | Keep raw tree read-only; use non-interactive parsers only; add qpdf/static scan when available; never execute embedded content. | mitigated, residual open |
| R-015 | KVQuant has no root license file; three embedded Transformers-derived trees have fixed parent-tree hashes but unresolved upstream commits, and the README attributes GPTQ/GPTQ-for-LLaMA/SqueezeLLM reuse. | Wrong reference lineage, compatibility drift, or license/notice noncompliance. | B-006/E10 map exact upstream commits and patch deltas and establish license authority before reference execution, copying, or publication. | open |
| R-016 | vLLM TurboQuant decode creates conversions/rotations and has fallback allocation branches. | Measured-region allocation or graph-dependent behavior. | Allocation hooks and graph replay tests must cover every selected preset and shape. | open |
| R-017 | KIVI source uses torch.cat for cache growth and may specialize kernels for group sizes different from the planned 32. | Direct reference timing is unfair or configuration unsupported. | Reference is correctness-only; build a preallocated adapter and prove group-size-32 semantics with fixtures. | open |
| R-018 | KVQuant reference computes top-k/outlier masks dynamically and uses explicit synchronization in its harness. | CPU sync, dynamic allocation, and contaminated timing. | Move calibration/selection outside measurement; use fixed-cap buffers; never import reference timings. | open |
| R-019 | Repository files are owned by root in the current environment. | A later non-root operator may be unable to update the project. | Verify ownership in environment/bootstrap setup before Phase 1 writes; do not recursively change ownership without operator approval. | open |
| R-020 | Cloudflare REST verifies bucket `kvbench-artifacts` exists, r2.dev is disabled, zero custom domains are configured, and exact enabled rule `kvbench-evidence-indefinite` covers `kvbench/sha256/` with Indefinite retention. Synthetic root `bbb80210dc729dedc9dd25a24d61cfbedbbe9d05661b1f95e6af278df3d0c11e` and 222-object container-G0 root `85e1f49dea76d08b2cba4477d089a71759d529f03b2bc3538da3d15d8639455c` both cleanly retrieve with valid COMPLETE, inventory, checksums, and no unexpected object. | Future provider-policy, prefix, credential, or object-set drift could make later evidence exposed or unauditable. | Retain read-only public/lock verification before every publication, conditional creation, exact-existing verification, COMPLETE-last ordering, and clean retrieval. Any changed bucket/prefix/lock condition fails closed. | mitigated for exact verified R2 acceptance; future-state drift remains gated |
| R-021 | The selected KIVI official-repository commit postdates arXiv v2 and includes later GQA changes. | Candidate fixtures may silently validate post-paper rather than paper-era semantics. | E07 documents the equivalence comparison and requires a decision before accepting fixtures; otherwise preserve both scopes or exclude. | open |
| R-022 | Phase 6A built and scanned exact Docker image ID / OCI image-index digest `sha256:059bc9be89387369d7de9e3e9b26d85b6e9902c41e7dbf002ebc45edd188fb7e`. Container G0 and separate BF16 eager/graph parity smokes pass with exact GPU, package/tool, model, backend, numerical, GQA, allocation, and graph controls. Decision 0016 makes the digest authoritative and tags non-authoritative. | A changed image digest or unrecorded runtime substitution could reintroduce CUDA/environment drift. | Permit Measurement Lane CUDA only inside the Decision 0016 digest. A changed digest requires new G0, both parity smokes, durable publication/retrieval, and a new authority decision. | mitigated for exact authorized image; digest-change risk remains fail-closed |
| R-023 | The original high-level trace could not prove device dispatch. Both complete post-B-016 campaigns preserve 80 raw operation bundles. Reporting commit `7f72c95f9932c608f9bd68f1971d6e86378596a2` independently replays their device traces, source/shape evidence, allocation joins, and exact operation keys; every operation derives `gqa_nonmaterialization_verified` for the Flash split-K GQA/MHA family with no failure reason. | A hidden GQA expansion, unproven dispatch, or stale report join could invalidate the common baseline. | Preserve all raw bundles and the generator-SHA-bound rederivation tests. New report `phase3-g1-20260723t132609515797z-7f72c95f-f31ccb` passes all three affected dispatch/source/backend criteria. | mitigated for B-011 and native-host G1 2026-07-23 |
| R-024 | Decisions 0013/0014 attribute the frozen backend's bounded common-path eager allocations with geometry-specific split controls. The new report independently replays 72 eager operations with 1,066 attributed events each and 8 graph operations with strict zero events. | Invalid eager timing, weakened graph allocation policy, or a false report verdict could make the baseline unusable. | Preserve the raw event evidence, exact eager criterion, strict graph-zero criterion, and missing/tampered/contradictory-evidence tests. | mitigated for B-012 and native-host G1 2026-07-23 |
| R-025 | The original terminal `[No data]`/missing-`pmon` race and three first-remediation failures remain immutable. After the B-013 fix, the exact race recurred in eight latest-campaign snapshots; each joined to the registered GPU/PID/start time and passed as `owned_only`. | Missing exact-run evidence or accidental relaxation of exclusivity checks. | Retain exact registered-row matching, PID start-time protection, and hard failures for unknown-unregistered, foreign, and PID-reuse observations. | resolved for B-013 2026-07-23 |
| R-026 | The report publisher has staging validation, source checksum/immutability revalidation, COMPLETE-last, atomic no-replace promotion, tamper tests, and a realistic 20-run fixture. It preserved the B-017 FAIL report and independently published new PASS report `phase3-g1-20260723t132609515797z-7f72c95f-f31ccb`; all ledger entries validate, no payload is newer than `COMPLETE`, and source runs remain unchanged. | A source-join or lifecycle regression could produce unusable evidence. | Preserve every old and failed report plus the new PASS report; retain independent rederivation and publication lifecycle tests. | mitigated for actual Phase 3 publication 2026-07-23 |
| R-027 | Six first-remediation workers exposed post-audit checksum mismatch. Untimed exact-endpoint diagnostics proved stable outputs and identified inconsistent checksum serialization rather than a model-output difference. All 13 completed B-015 fixed-L runs preserve exact audit/measured equality. | A checksum semantic mismatch could falsely reject a stable retained callable. | Retain the canonical shape/dtype-bound tensor checksum and full-repeat eager/graph regression under the frozen numerical contract. | resolved for B-014 2026-07-23 |
| R-028 | The second-remediation raw-audit producer exceeded the old run hard limit twice and hid thirteen lower producer causes. Decision 0014 used untimed source-backed diagnostics to bind a 1,152 MiB envelope and geometry-specific split controls. The B-015 campaign had no bound failure or generic cause; all three failures preserved their exact graph trace error. | Required B-011/B-012 coverage can abort before measurement, and a bound-only change could mask an evidence-volume or collection defect. | Retain the exact-bound/+1-byte, exception-preservation, and split-control regressions. | mitigated for B-015 2026-07-23 |
| R-029 | Three B-015 fixed-L CUDA Graph audits produced valid asynchronous GPU ranges which outlived their host launch marker. Decision 0015 corrected only that scope relation while retaining stricter launch/event bounds. Both complete post-fix campaigns finished; all 80 operation replays identify the required Flash family and all 8 graph operations pass strict zero allocation. | Missing direct device dispatch and zero-allocation evidence, incomplete campaigns, or an invalid proof if asynchronous scoping were weakened. | Retain Decision 0015, deterministic fail-closed parser controls, the actual long CUDA control, and the immutable campaign evidence. | mitigated for B-016 and complete campaign execution 2026-07-23 |
| R-030 | The B-017 FAIL report exposed that legacy report criteria did not consume the simplified consolidated raw-audit contract. Reporting commit `7f72c95f9932c608f9bd68f1971d6e86378596a2` now selects behavior by recorded generator SHA, reconstructs execution source bytes, binds sidecar/index/operation/file digests, invokes the existing coordinator replay, and ignores worker verdict booleans. | A scientifically valid or invalid campaign could receive a report verdict disconnected from mandatory raw B-011/B-012 evidence. | Retain targeted local-pass/worker-fail, local-fail/worker-pass, missing, tampered, and mismatched-index tests. Preserve both the old FAIL report and the independently validated new PASS report. | mitigated for B-017 2026-07-23 |
| R-031 | A common adapter could silently change BF16 cache geometry, backend selection, timing boundaries, byte ownership, or Graph behavior while appearing mechanically equivalent. | The validated baseline could drift before later methods reuse the interface, invalidating comparisons and admission evidence. | Bind adapter source/model/backend/cache layout in the deterministic fingerprint; keep method selection before measurement; retain full-model golden, allocation, GQA, graph, runner-delegation, and fail-closed factory tests for every adapter. | mitigated for Phase 4 BF16; open for every future method |
| R-032 | Phase 5 fixtures depend on a large isolated vLLM environment and kernel-name traces. Exact package/source hashes, SM120 identity, per-file ledgers, byte-layout validation, and identical no-replace regeneration pass; profiler durations are discarded. Direct graph capture is not exercised. | Environment drift, fixture overwrite, or treating reference trace names as performance/HBM evidence could invalidate later adapter golden tests. | Retain source/environment locks and no-replace validation; use fixtures only for Phase 6 correctness. Repeat allocation, sanitizer, execution-path, and graph capture/replay admission in the future Measurement Lane before G2-TQ. | mitigated for deterministic Phase 5 fixtures; Phase 6 admission open |

| R-033 | Final Phase 6 run `phase6-20260725t065153714z-ace9261a-083f14-4bit_nc-fixed-l128-eager` passes functional store/append/decode but Compute Sanitizer reports 2,093,260 leaked bytes in 28 allocations at context teardown. Earlier fail-closed attempts remain immutable. | G2-TQ could be falsely admitted if functional success were confused with zero-error sanitizer completion, or if leak criteria were loosened post hoc. | Retain B-018, the unique zero ERROR/LEAK summary parser, exact image and source authority, and all failed evidence. Remediate only resource lifetime, then use new IDs for all three mandatory sanitizer probes before any bounded grid point. | open; blocks G2-TQ |
