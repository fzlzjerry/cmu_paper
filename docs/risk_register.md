# Risk register

Last updated: 2026-07-22 after Phase 3 BF16 G1 FAIL.

| ID | Risk and evidence | Potential impact | Required mitigation / gate | Status |
|---|---|---|---|---|
| R-001 | Formal E00 run `e00-20260722T041628.190813Z-980eff7b6f59-0dd71f2d` materialized a toolchain gap when SASS dumping could not find `nvdisasm`. The failed evidence remains immutable. | Build/inspection failure, unavailable native/PTX proof, invalid counters, or wrong code path. | Exact package/tool locking at 6442ba1f7554ea0ebf0b3bb1a920c94567cab689 and full rerun `e00-20260722T050632.375718Z-6442ba1f7554-02d5bd32` passed SASS/PTX inspection, native and forced-PTX execution, numerical/Graph/allocation checks, and all sanitizer lanes. Container parity remains separately tracked by R-022/B-010. | mitigated for native-host G0 2026-07-22 |
| R-002 | CUDA Graph support differs by method; KIVI/KVQuant references are dynamic, while vLLM TurboQuant only declares uniform-batch support. The Phase 3 BF16 fixed-shape lane passed 8/8 capture/replay, output-agreement, pointer-stability, and replay-allocation controls. | Capture failure or unfair graph/eager comparison. | Preserve the BF16 evidence, and require E04 plus each G2 to repeat capture/replay, output, pointer, and allocation validation for its own implementation; unsupported methods remain eager-only. | mitigated for the BF16 fixed-shape lane; method-specific risk open |
| R-003 | KIVI pinned source contains repeat_kv/expand-reshape paths; KVQuant GQA support is unestablished. | Physical KV replication and inflated traffic/memory. | GQA execution-path audit plus synthetic MHA control; reject materialization in Measurement Lane. | open |
| R-004 | vLLM TurboQuant has a continuation-prefill fallback that fully dequantizes cached K/V; other references may reconstruct large tensors. | False compression/speed conclusions and OOM. | Fixed-L trace must prove the fallback absent; G3 rejects any full-prefix BF16 temporary. | open |
| R-005 | KIVI pins Torch 2.1.2, Transformers 4.36.2, Triton 2.1.0, FlashAttention 2.5.6, and CUDA 12.1-era packages; KVQuant vendors Transformers 4.38.0.dev0. | Blackwell build/runtime incompatibility or accidental semantic drift during upgrades. | Isolate reference containers; record exact patches; do not modernize semantics silently. | open |
| R-006 | Full B/L grids up to B=16 and L=131072 may exceed 96GB after weights, cache, workspace, and safety margin. | OOM, selective missingness, or capacity/same-work confusion. | Precompute feasibility with max_memory_fraction=0.88 and record every infeasible point before launch. | open |
| R-007 | TurboQuant paper provides no author-owned repository; vLLM v0.25.1 is only a candidate reference and explicitly omits QJL. | Golden fixtures may validate a different algorithmic variant. | Resolve reference authority and record any chosen equivalence in a decision before E05 fixtures. | open |
| R-008 | KVQuant dataset, calibration seed, quantizers, sink policy, dtypes, and outlier cap are not frozen. | Data leakage, changing bytes, and non-reproducible method semantics. | E09 freezes all calibration inputs/artifacts before adapter or scan work. | open |
| R-009 | Archive acquisition URL and history before workspace delivery are unknown, although local bytes are fully hashed. | External reacquisition cannot reproduce the identical bundle. | Obtain operator provenance if available; retain archive hash and every extracted-file hash permanently. | open |
| R-010 | Git repository was initially unborn and had no SHA. | No run could satisfy code-provenance requirements before a commit existed. | Resolved by reviewed root commit 9569d938d9023a3e71d98f12234efa1897004533; every later run still requires a clean recorded SHA. | resolved 2026-07-22 |
| R-011 | The exact primary model/tokenizer identity was formerly unresolved. Decision 0007 now freezes `meta-llama/Llama-3.1-8B-Instruct` revision `0e9e39f249a16976918f6564b8830bc894c89659`, all 11 artifact hashes, 32/8 GQA geometry, and 131072 positions; offline BF16 load passed. | Unsupported head geometry, context limit, or altered attention path. | Preserve the exact model, tokenizer, hashes, and offline-loading policy for every later run and quality join. | resolved for identity 2026-07-22; runtime admission separately failed |
| R-012 | Profiler instrumentation changes timing. | Invalid latency claims. | Separate timing, nsys, and ncu run kinds and validate analysis filters. | controlled by contract |
| R-013 | Phase 3 captured GPU identity, power, clocks, thermals, and continuous process evidence per run. Three graph processes at fixed-L B1/L4096 had admission-only host-wall CV 0.0254%, but eager stability timing was absent and one terminal process audit failed closed. | High CV, biased replicates, or incomplete stability evidence. | Preserve every replicate; remediate B-013 and require both eager and graph summaries at CV <= 3% in the new full campaign. G5 remains NOT EVALUATED. | graph engineering control passed; overall timing risk open |
| R-014 | PDFs are untrusted binaries; qpdf is unavailable. Static checks found no JavaScript or attachments. | Parser exposure or incomplete active-content assessment. | Keep raw tree read-only; use non-interactive parsers only; add qpdf/static scan when available; never execute embedded content. | mitigated, residual open |
| R-015 | KVQuant has no root license file; three embedded Transformers-derived trees have fixed parent-tree hashes but unresolved upstream commits, and the README attributes GPTQ/GPTQ-for-LLaMA/SqueezeLLM reuse. | Wrong reference lineage, compatibility drift, or license/notice noncompliance. | B-006/E10 map exact upstream commits and patch deltas and establish license authority before reference execution, copying, or publication. | open |
| R-016 | vLLM TurboQuant decode creates conversions/rotations and has fallback allocation branches. | Measured-region allocation or graph-dependent behavior. | Allocation hooks and graph replay tests must cover every selected preset and shape. | open |
| R-017 | KIVI source uses torch.cat for cache growth and may specialize kernels for group sizes different from the planned 32. | Direct reference timing is unfair or configuration unsupported. | Reference is correctness-only; build a preallocated adapter and prove group-size-32 semantics with fixtures. | open |
| R-018 | KVQuant reference computes top-k/outlier masks dynamically and uses explicit synchronization in its harness. | CPU sync, dynamic allocation, and contaminated timing. | Move calibration/selection outside measurement; use fixed-cap buffers; never import reference timings. | open |
| R-019 | Repository files are owned by root in the current environment. | A later non-root operator may be unable to update the project. | Verify ownership in environment/bootstrap setup before Phase 1 writes; do not recursively change ownership without operator approval. | open |
| R-020 | Phase 2 now provides tested local no-replace finalization, strict inventories, SHA-256 ledgers, completion markers, provenance locking, and formal-evidence-root guards. Raw `/artifacts/` remains Git-ignored, and no durable append-only backing, external attestation, retention proof, or locator publication mechanism exists. | Local evidence can still be lost or coherently rewritten by the same storage principal; claim evidence would be unauditable without external retention and linkage. | B-009 blocks admission and claim-bearing runs until a durable store is exercised and an immutable locator plus manifest/ledger digest is published and retrieval-verified. | locally mitigated; durable risk open |
| R-021 | The selected KIVI official-repository commit postdates arXiv v2 and includes later GQA changes. | Candidate fixtures may silently validate post-paper rather than paper-era semantics. | E07 documents the equivalence comparison and requires a decision before accepting fixtures; otherwise preserve both scopes or exclude. | open |
| R-022 | Native-host G0 has no container digest and cannot certify the eventual measurement image. Phase 3 timing is explicitly non-claim native-host admission evidence. | Later CUDA behavior could differ despite host certification. | B-010 requires the identical preflight to pass in a digest-pinned measurement container after E01 and before formal E02 closure, ordinary timing, later method admission, or any claim. | open |
| R-023 | Phase 3 static source, tensor-shape, and storage audits found KV-head geometry and no query-head-sized temporary, but all 79 GQA SUT operator audits exposed only the high-level SDPA op and therefore failed the frozen fused-kernel proof. All 19 MHA controls failed the same lower-level proof. | A hidden GQA expansion or unproven dispatch could invalidate the common baseline. | B-011 requires direct fused-kernel/dispatch evidence for every executed GQA geometry/lane and the corresponding MHA control; fail closed until proven. | materialized; open |
| R-024 | Eager exact decode issued 1,066 allocator events in each fixed-L audit or 17,056 in each 16-step growing-context audit, with cumulative bytes ranging from 10,960,908 to 1,005,810,432. Both batch sizes occur in each group. Persistent deltas were zero, but the contract rejects transient events too. | Invalid eager timing and an unusable common baseline. | B-012 requires preallocation/elimination and a new complete campaign; graph zero-allocation evidence does not waive eager failure. | materialized; open |
| R-025 | In one terminal sample, `compute_apps` reported the previously supervised worker PID as `[No data]` while `pmon` no longer listed it; the coordinator recorded it unknown and aborted the point. Five preceding samples had identified the same PID/start ticks as supervised, and after/release snapshots were clean. | Missing exact-run evidence or accidental relaxation of exclusivity checks. | B-013 requires race-safe identity classification while retaining fail-closed foreign/unknown detection. | materialized; open |
| R-026 | The G1 report writer creates `COMPLETE` before its post-write independent validation. The actual report rederives successfully from all 20 source runs, but a future validation failure could strand a locally immutable invalid bundle. The real artifact root exercises the full join; the unit suite does not yet contain a self-contained realistic 20-run fixture. | Future report publication could retain unusable local evidence or allow a source-join regression to escape unit tests. | Before the next admission report, validate a staging bundle before final completion or define a fail-closed quarantine lifecycle, and add a realistic end-to-end report fixture. Preserve the current checksum-valid report. | existing report valid; hardening open |
