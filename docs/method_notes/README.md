# Method notes

This directory is reserved for reviewed, source-pinned method-equivalence and
implementation notes created in later phases. Phase 2 does not contain method
adapters, kernels, benchmark results, or inferred upstream identities.

Any future note must distinguish reference behavior from measurement-adapter
behavior and link to immutable source and configuration fingerprints.
